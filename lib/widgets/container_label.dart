import 'package:flutter/material.dart';

class ContainerLabel extends StatelessWidget {
  const ContainerLabel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
